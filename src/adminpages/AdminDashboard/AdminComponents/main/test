<Box
    sx={{
      display: { xs: 'flex', md: 'grid' },
      gridTemplateColumns: 'repeat(12,1fr)',
      gridAutoRows: 'minmax(50px, auto)',
      // gridTemplateColumns: 'repeat(4,1fr)',
      // gridAutoRows: 'minmax(100px, auto)',
      gap: 3,
      // marginTop: '20px',
      textAlign: 'center',
      flexDirection: 'column',
    }}
  > 
    
    <Paper elevation={3} sx={{ p: 3, gridColumn: '2/5', gridRow: '1'}}>
      <Typography variant="h6">Total Grievances</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      > 
        <Divider/>
        <Typography variant="h4">24</Typography>
      </Box>
    </Paper>
    <Paper elevation={3} sx={{ p: 3, gridColumn: '5/8', gridRow: '1'}}>
      <Typography variant="h6">Total Grievances</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      > 
        <Divider/>
        <Typography variant="h4">24</Typography>
      </Box>
    </Paper>
    <Paper elevation={3} sx={{ p: 3, gridColumn: '8/11', gridRow: '1'}}>
      <Typography variant="h6">Total Grievances</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      > 
        <Divider/>
        <Typography variant="h4">24</Typography>
      </Box>
    </Paper>
    <Paper elevation={3} sx={{p:3, gridColumn:'1', gridRow: '2'}}>
        <GrievanceChart/>
    </Paper> 
    <Paper elevation={3} sx={{p:3, gridColumn:'1', gridRow: '3'}}>
        <GrievanceChart/>
    </Paper> 
    <Paper elevation={3} sx={{p:3, gridColumn:'1', gridRow: '4'}}>
        <GrievanceChart/>
    </Paper> 

    {/* <Paper elevation={3} sx={{ p: 3, gridColumn: '2/5', gridRow: '1'}}>
      <Typography variant="h6">Total Grievances</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
       <Group sx={{ height: 100, width: 100, opacity: 0.3, mr: 1 }} /> 
        <Divider/>
        <Typography variant="h4">24</Typography>
      </Box>
    </Paper> */}
  
  

  


    {/* <Paper elevation = {3} sx={{p:3, gridColumn: '1/10', gridRow: '1/ span 3'}}>
      <Completed/>
    </Paper> */}




    {/* <Paper elevation={3} sx={{ p: 3, gridColumn: '10/13', gridRow: '1/ span 1' }}>
      <Typography variant="h4">Total Queries</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <Group sx={{ height: 100, width: 100, opacity: 0.3, mr: 1 }} />
        <Typography variant="h4">24</Typography>
      </Box>
    </Paper>

    <Paper elevation={3} sx={{ p: 3, gridColumn: '10/13' }}>
      <Typography variant="h4">Total Grievances</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <MapsHomeWork sx={{ height: 100, width: 100, opacity: 0.3, mr: 1 }} />
        <Typography variant="h4">22</Typography>
      </Box>
    </Paper>

    <Paper elevation={3} sx={{ p: 3, gridColumn: '10/13' }}>
      <Typography variant="h4">Total Suggestions</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <MapsHomeWork sx={{ height: 100, width: 100, opacity: 0.3, mr: 1 }} />
        <Typography variant="h4">23</Typography>
      </Box>
    </Paper>
     */}
    
    {/* <Paper elevation={3} sx={{p:3, gridColumn:'1'}}>
      <GrievanceChart/>
    </Paper> 


    <Paper elevation={3} sx={{ p: 3 }}>
      <Typography variant="h4">Total Suggestions</Typography>
      <Box
        sx={{
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <MapsHomeWork sx={{ height: 100, width: 100, opacity: 0.3, mr: 1 }} />
        <Typography variant="h4">23</Typography>
      </Box>
    </Paper>
    <Paper elevation ={3} sx={{p: 3, gridRow: '2', gridColumn: '1/2'}}>
        <GrievanceBarGraph/>
    </Paper>
    <Paper elevation ={3} sx={{p: 3, gridRow: '2', gridColumn: '2/5'}}>

</Paper> */}

  </Box>